FROM node:20-alpine

WORKDIR /app

# Instalar dependencias
COPY package*.json ./
RUN npm ci || npm install

# Copiar código fuente
COPY . .

# Exponer puerto para Vite dev server
EXPOSE 5177

# Comando para desarrollo con hot reload
CMD ["npm", "run", "dev"]

